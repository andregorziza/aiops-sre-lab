version: "3.9"

services:
  app:
    build: ./app
    container_name: aiops-sre-app
    ports:
      - "3000:3000"
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    container_name: aiops-sre-prometheus
    volumes:
      - ./observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
    ports:
      - "9090:9090"
    depends_on:
      - app

  grafana:
    image: grafana/grafana:latest
    container_name: aiops-sre-grafana
    ports:
      - "3001:3000"
    depends_on:
      - prometheus

